# Wiki 演化日志

本日志原则上只追加，记录已经实际写入的 ingest、产生写回或重要发现的 query、lint、schema 变更与迁移。普通查阅、未执行方案、取消和否决不记录。

标题格式：

```text
## [YYYY-MM-DD HH:MM] ingest | 简短标题
```

旧记录存在实质错误时追加 `correction`，不要重写历史；仅允许直接修复不改变含义的错别字或失效路径。
